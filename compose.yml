services:
  backend:
    build: backend
    ports:
      - 8000:8000
    healthcheck:
      test: ["CMD", "curl", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    